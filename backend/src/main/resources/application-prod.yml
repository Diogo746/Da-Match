spring:
  application:
    name: fullstack-api-damatch

server:
  # Render injects PORT for web services. The default preserves local execution.
  port: ${PORT:8080}

app:
  environment: production

management:
  endpoints:
    web:
      exposure:
        include: health,info
